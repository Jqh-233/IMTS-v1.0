# 开发日志 2026-06-07

## 完成事项

### 第一阶段：数据库层统一
- [x] 统一 DB 路径到 `database.py` 单入口，删除 `api/emails.py` 中独立路径
- [x] `mail_sync_service.py`：`save_email()`、`mark_email_processed()`、`sync_sample_emails()`、`sync_qq_recent_emails()` 全部从 raw sqlite3 迁移到 SQLAlchemy ORM
- [x] `api/emails.py`：`sync_demo()` 和 `sync_qq()` 端点重写为纯 ORM，事务由 `db.commit()`/`db.rollback()` 管理
- [x] 删除死代码：`services/task_service.py`（9 个函数中 7 个从未调用）、`data/database.py`（raw 建表）、`data/sample_emails.py`（数据内联到 mail_sync_service.py）
- [x] `main.py`：建表逻辑从模块顶层迁移到 lifespan 启动事件
- [x] 清理无用依赖：移除 `aiosqlite`、`aiofiles`

### 第二阶段：异常处理 + 日志系统
- [x] 新建 `logging_config.py`：模块级 logger 工厂 + 统一格式（时间戳 + 级别 + 模块名）
- [x] `api/emails.py`：`except Exception: pass` 改为 `logger.warning(…, exc_info=True)` + 失败计数 + 不标记已处理（允许重试）
- [x] `ai/task_extractor.py`：LLM 降级到规则引擎时记录 `logger.warning`
- [x] `api/config.py`：`except Exception:` → `except ValueError:`（精确异常类型）

### 第三阶段：前端修复
- [x] 添加 `dayjs` 为 `package.json` 显式依赖（消除隐式传递依赖风险）
- [x] `TaskForm.vue`：内部 `submitting` ref → 父组件 `loading` prop，修复按钮 loading 闪烁 bug
- [x] 导出 `CreateTaskPayload`/`UpdateTaskPayload` 类型，消除 `Board.vue` 中 `as any` 类型绕过
- [x] `emails.ts`：新增 `forceTask()` action，`EmailDrawer.vue` 不再直接修改 `store.emails` ref

### 第四阶段：测试基础设施
- [x] 安装 pytest + pytest-cov + httpx
- [x] 新建 `backend/tests/conftest.py`：pytest fixtures（数据隔离 + TestClient）
- [x] 新建 `backend/tests/test_task_crud.py`：10 个测试用例，覆盖全部 7 个 CRUD 端点
- [x] 10/10 测试通过，`vue-tsc --noEmit` + `vite build` 通过

### 文档更新
- [x] PROJECT_OVERVIEW.md、docs/architecture.md、docs/development-plan.md 同步至最新状态
- [x] memory/project-status.md、memory/optimization-plan.md 标记完成
- [x] FUTURE_OPTIMIZATION.md 新增测试扩展和日志增强两个方向

## 待办事项

---

## 补充（同日）：第六步补充 — Bug 修复与基础设施加固

基于全面代码审计（9 角度查找 + 验证 + 缺口扫描）发现的 6 个严重/高优问题：

### 6.7 测试数据库隔离
- [x] `conftest.py` 重写：内存 SQLite (`:memory:`) + `StaticPool` + FastAPI `dependency_overrides`
- [x] `_setup_db` autouse fixture：每个测试前建表、后删表，完全隔离
- [x] 与生产 `imts_demo.db` 零接触，10/10 测试通过

### 6.8 Alembic 数据库迁移
- [x] 新增 `alembic>=1.14.0` 依赖
- [x] `alembic init alembic` → 配置 `env.py`（Base.metadata + DATABASE_URL）
- [x] 生成初始迁移 `a7a6b5310c24_initial.py`（建表 emails + tasks）
- [x] `main.py` lifespan 改为 `alembic upgrade head`
- [x] 验证：全新启动建表 ✅ + 旧数据库 stamp 升级 ✅

### 6.9 同步逻辑修正
- [x] `mail_sync_service.py`：`sync_sample_emails` 和 `sync_qq_recent_emails` 返回值从 `(all_ids, new_ids)` 改为只返回 `new_ids`
- [x] `emails.py` sync_demo/sync_qq：`for eid in new_ids:` 替代 `for eid in all_ids:`
- [x] 删除 `if email.is_processed: continue` 检查（new_ids 中天然未处理）
- [x] 删除 `mark_email_processed()` 调用（不再需要同步标记）
- [x] 提取失败 `continue` 不设 is_processed，用户可在邮件列表手动重试
- [x] `tasks.py` delete_task：删任务后重置关联邮件 `is_processed=0`，允许重新提取
- [x] 验证：重复同步返回 `synced_emails: 0`，删任务后可重新提取 ✅

### 6.10 前端 Bug 修复
- [x] `TaskForm.vue`：`const isEdit = !!props.task` → `computed(() => !!props.task)`
- [x] 修复编辑弹窗标题始终显示"创建任务"、状态字段不显示的 Bug

### 6.11 CORS 配置修正
- [x] `main.py`：`allow_origins` 从环境变量 `CORS_ORIGINS` 读取（默认 `http://localhost:5173`）
- [x] `.env.example` 新增 `CORS_ORIGINS` 配置项
- [x] 支持逗号分隔多域名

### 验证结果
- [x] `pytest tests/ -v`：10/10 通过
- [x] 全新启动 → Alembic 建表 → /health ok
- [x] 演示邮件同步 15 封 → 提取 8 个任务 → 重复同步 0 封
- [x] 删任务 → 重提取成功

### 审计遗留
- [x] 12 项低/中优先问题录记入 FUTURE_OPTIMIZATION.md 第 11 节